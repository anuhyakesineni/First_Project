import sun.security.util.Password
/*
class PasswordGenerator(){
    var seed:String ?=null
    var hash=null
    var hashRepititions=null
}


fun main(){
    val generator = PasswordGenerator().run {
        seed = "someString"
        hash = { s -> someHash(s) }
        hashRepititions = 1000
    }
    val password: Password = generator.generate()
}

val generator = PasswordGenerator().run {
    seed = "someString"
    hash = { s -> someHash(s) }
    hashRepititions = 1000
}
val password: Password = generator.generate()
*/